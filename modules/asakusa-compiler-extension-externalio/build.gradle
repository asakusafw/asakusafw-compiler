description 'Asakusa DSL Compiler Extension for External I/O'

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'eclipse'

useSLF4J();

dependencies {
    compile project(':asakusa-compiler-model')
    compile project(':asakusa-compiler-api')
    compile "com.asakusafw:asakusa-windgate-vocabulary:${asakusafwVersion}"

    testCompile project(':asakusa-compiler-mock')
    testRuntime "com.asakusafw:asakusa-runtime:${asakusafwVersion}"
    testRuntime "org.apache.hadoop:hadoop-core:${hadoopVersion}"
}
